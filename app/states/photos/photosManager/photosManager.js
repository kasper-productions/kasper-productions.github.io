/**
 * Created by kawika on 9/29/17.
 */
var app = angular.module('kApp.photosManager', []);

app.factory('photosManager',
    function () {

        var photosManager = {
            //TODO CREATE album object and photo object
            //TODO make an array of albums that contains name and array of photos
        };

        return photosManager;

    }
);